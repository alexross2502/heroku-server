{
   
    "scripts": {
      "server-start": "cd server && npm install && npm start",
      "client-start": "cd web && npm install && npm start",
      "start": "npm run client-start && npm run server-start"
    }
 
  }